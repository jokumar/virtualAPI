template header
objectType
when_rule
then_rule
rule_name


template "ageRule"

rule "ageRule_@{rule_name}"
    when
        $x :  @{objectType}(@{when_rule})
    then
     @{then_rule};
end
end template

 